 select
     ROUND(( 1 - ( c.value / (a.value+b.value)))*100,2) AS Bfr_hit_Rto
    from
    v$sysstat a,
    v$sysstat b,
    v$sysstat c
where
 a.name='db block gets'
AND b.name='consistent gets'
AND c.name='physical reads';